export default function ProyectosPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Proyectos</h1>
      <p className="mt-2 text-sm text-foreground/70">
        Listado de proyectos y subproyectos con filtros por ID, cliente, zona y estado
        (Contacto → Campo → Cálculo → Dibujo → Entrega). Ver{" "}
        <code>docs/REQUIREMENTS.md</code> sección 4.2.
      </p>
    </div>
  );
}
