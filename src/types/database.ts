/**
 * Tipos de la base de datos generados por Supabase.
 *
 * Este archivo es un PLACEHOLDER inicial. Una vez tengas el proyecto de
 * Supabase creado y la migración `supabase/migrations/0001_init.sql`
 * aplicada, genera los tipos reales con:
 *
 *   npx supabase gen types typescript --project-id <tu-project-id> > src/types/database.ts
 *
 * Mientras tanto, `Database` se deja como `any` tipado mínimamente para que
 * el resto del proyecto compile.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
